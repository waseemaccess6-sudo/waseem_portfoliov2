document.addEventListener('DOMContentLoaded',()=>{for(const [id,file] of [['header-slot','partials-header.html'],['footer-slot','partials-footer.html']]){const el=document.getElementById(id);if(!el)continue;fetch(file).then(r=>r.text()).then(html=>{el.outerHTML=html;window.dispatchEvent(new Event('partials-ready'))}).catch(()=>{});} });window.addEventListener('partials-ready',()=>{const path=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('[data-page]').forEach(a=>{a.classList.toggle('active',a.dataset.page===path)});const header=document.querySelector('.site-header');const menu=document.querySelector('.menu-toggle');const drawer=document.querySelector('.mobile-drawer');const close=()=>drawer?.classList.remove('open');menu?.addEventListener('click',()=>drawer?.classList.add('open'));drawer?.querySelector('.backdrop')?.addEventListener('click',close);drawer?.querySelector('.mobile-close')?.addEventListener('click',close);drawer?.querySelectorAll('a').forEach(a=>a.addEventListener('click',close));const onScroll=()=>header?.classList.toggle('scrolled',scrollY>12);onScroll();addEventListener('scroll',onScroll,{passive:true});});
